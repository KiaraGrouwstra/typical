{
  "name": "typical-ts",
  "version": "0.0.2",
  "description": "",
  "files": ["lib"],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "pretest": "./node_modules/tsst/dist/build.js -b \"**/*.test.ts\" -d ./lib",
    "test": "mocha lib/**/*.test.js 2>&1 | sed 's/[0-9]\\+)/×/g' | tee errors.log",
    "clean": "rm -rf lib/*",
    "build": "npm run clean && tsc"
  },
  "author": "Tycho Grouwstra",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^2.2.41",
    "mocha": "^3.5.0",
    "tsst": "github:TheOtherSamP/ts-semantic-testing",
    "typescript": "github:microsoft/typescript"
  }
}
